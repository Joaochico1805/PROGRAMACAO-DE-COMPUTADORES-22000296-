#include <stdio.h>
#include <stdlib.h>

// Um armazém trabalha com 100 mercadorias diferentes identificadas pelos números inteiros de 1 a 100. O dono do armazém anota a quantidade de cada mercadoria vendida durante o mês. Ele tem uma tabela que indica para cada mercadoria o preço de venda. Escreva o algoritmo para calcular o faturamento mensal do armazém, isto é: A tabela de preço e quantidades são fornecidas em dois conjuntos, sendo que um conjunto contém a quantidade vendida e o outro o preço de cada mercadoria.

int main(int argc, char const *argv[])
{



    return 0;
}