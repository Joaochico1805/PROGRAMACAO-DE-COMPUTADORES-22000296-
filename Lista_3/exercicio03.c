#include <stdio.h>
#include <stdlib.h>

// Escrever um algoritmo que lê um vetor X(100) e depois o escreve. Substitua, a seguir, todos os valores iguais a zero do vetor X por 1, e escreva novamente o vetor X.

int main(int argc, char const *argv[])
{



    return 0;
}